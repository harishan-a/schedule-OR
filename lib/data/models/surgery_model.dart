// Re-export Surgery model from its current location.
// This allows new code to import from data/models/ while existing code
// continues to work with the old import path.
// In Wave 5, the actual model will be moved here.
export 'package:firebase_orscheduler/features/schedule/models/surgery.dart';
